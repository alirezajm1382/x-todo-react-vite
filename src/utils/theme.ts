import { extendTheme } from "@mui/joy";

const theme = extendTheme({
  colorSchemes: {
    dark: {
      palette: {
        background: {
          body: "#0d0714",
        },
      },
    },
    light: {
      palette: {
        background: {
          body: "#0d0714",
        },
      },
    },
  },
});

export default theme;
